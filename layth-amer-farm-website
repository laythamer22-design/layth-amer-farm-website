generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum BookingType {
  STANDARD
  TRIAL
}

model Booking {
  id            String      @id @default(uuid())
  date          DateTime
  customerName  String
  customerPhone String
  visitorType   String      // "عائلة" أو "شباب"
  type          BookingType @default(STANDARD)
  status        String      @default("PENDING")
  identityUrl   String?
  receiptUrl    String?
  createdAt     DateTime    @default(now())
}

model FarmSettings {
  id            Int      @id @default(1)
  farmKnowledge String   @db.Text
  updatedAt     DateTime @updatedAt
}
